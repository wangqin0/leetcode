Unix:
```
LC_TARGET=141_linked_list_cycle && cmake --build ./build --target $LC_TARGET && ./build/src/$LC_TARGET
``

Windows:
```
$LC_TARGET="141_linked_list_cycle"; cmake --build build --target "$LC_TARGET" && . ".\build\src\$LC_TARGET"
```
